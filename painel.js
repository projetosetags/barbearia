(()=>{const cfg=window.BARBEARIA_CONFIG||{};const el=id=>document.getElementById(id);const hoje=new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());window.addEventListener('DOMContentLoaded',()=>{if(el('dataPainel'))el('dataPainel').value=hoje;if(!cfg.supabaseUrl||!cfg.supabasePublishableKey){if(el('loginTexto'))el('loginTexto').textContent='Painel preparado. Aguardando apenas a conexão do novo banco exclusivo da Barbearia.';if(el('btnEntrar'))el('btnEntrar').onclick=()=>{const m=el('loginMsg');m.textContent='Banco novo ainda não conectado.';m.className='mensagem warn'}}})})();